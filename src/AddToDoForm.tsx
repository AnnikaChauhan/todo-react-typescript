import React from 'react';

export const AddToDoForm = props => {
    return (
        <form>
            <input type="text"/>
            <button type="submit">Add To Do</button>
        </form>
    );
}